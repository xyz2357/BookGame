# 开发进度与技术实现

> 本文档记录实际开发中做出的技术决策和当前进度，供后续开发查阅。

## 技术选型（已确定）

| 项 | 决策 | 原因 |
|---|---|---|
| 语言 | TypeScript | 速度快、类型安全 |
| 框架 | React 19 + Vite | UI 开发方便，热更新快 |
| 状态管理 | React Context + useReducer | 够用，不引入额外库 |
| 单元测试 | vitest | 与 Vite 原生集成 |
| E2E 测试 | Playwright (Chromium) | 模拟真实用户操作 |
| 构建 | Vite 8 | 开发快、构建快 |

**不用**：Godot、Ren'Py、Unity（原文档推荐 Godot，实际选了 Web 方案）

## 实际项目结构

```
BookGame/
├── DesignDoc/                    # 设计文档
├── src/
│   ├── core/                     # 纯 TS 核心逻辑（不依赖 React）
│   │   ├── types.ts              # 所有类型定义
│   │   ├── DataLoader.ts         # JSON 数据加载 + 查询
│   │   ├── GameState.ts          # 纯函数式状态管理
│   │   ├── EventResolver.ts      # 事件解析（book + event → outcome）
│   │   ├── EffectApplier.ts      # Effect 执行器
│   │   ├── EndingJudge.ts        # 结局判定
│   │   └── __tests__/            # 单元测试 + 集成测试 + 数据完整性测试
│   ├── components/               # React UI 组件
│   │   ├── App.tsx               # 屏幕路由
│   │   ├── MainMenu.tsx
│   │   ├── NodeView.tsx          # 节点视图（含 HP 显示）
│   │   ├── EventView.tsx         # 事件视图（选书 + 结果）
│   │   ├── InventoryView.tsx     # 背包
│   │   └── EndingView.tsx        # 结局
│   ├── context/
│   │   └── GameContext.tsx        # 状态管理中枢
│   ├── data/                     # JSON 数据
│   │   ├── books.json
│   │   ├── events.json
│   │   ├── nodes.json
│   │   ├── endings.json
│   │   └── strings.json
│   ├── main.tsx
│   ├── index.css
│   └── vite-env.d.ts
├── tests/                        # Playwright 浏览器测试
│   ├── golden-path.spec.ts
│   ├── tag-match-path.spec.ts
│   ├── failure-and-retry.spec.ts
│   └── game-over.spec.ts
├── playwright.config.ts
├── vite.config.ts
├── tsconfig.json
└── package.json
```

## 与设计文档的差异

### 新增：HP 血量系统

设计文档原本明确"不做 HP"，但开发中发现用错书直接卡死体验太差，加入了轻量 HP：

- 初始 3 滴血（❤ ❤ ❤）
- 用错书（default_outcome）= 掉 1 血 + 可以换书重试
- 用对书 = 不掉血，事件正常完成
- 血归零 = 进入"结局：迷失"（game over）
- 顶部常驻 HP 显示

**设计意图**：不是"战斗系统"，只是对试错的温和惩罚。3 次机会足够探索，不会造成焦虑。

### EventResolver 返回值变化

原设计只返回 Outcome，实际实现返回 `ResolveResult`：
```typescript
interface ResolveResult {
  outcome: Outcome;
  matchType: "super_match" | "tag_match" | "default";
}
```
这样 UI 层可以根据匹配类型给不同反馈（失败可重试、超级匹配可加特效）。

### 结局判定时机

不是每次事件结束都判定，而是**当前节点没有未完成事件且没有可走路径时**才触发结局判定。避免过早触发兜底结局。

## 测试体系

| 层 | 命令 | 文件数 | Tests | 说明 |
|---|---|---|---|---|
| Unit (fixture) | `npm test` | 4 | 40 | 用假数据，内容变不影响 |
| DataLoader | `npm test` | 1 | 6 | 验证 JSON 加载正确 |
| 集成 e2e | `npm test` | 1 | 8 | 用真实数据模拟通关 |
| 数据完整性 | `npm test` | 1 | 12 | 引用校验 + 可达性分析 |
| Playwright | `npm run test:e2e` | 4 | 4 | 浏览器用户操作模拟 |

**内容变化时**：
- Unit tests → 不受影响
- 数据完整性测试 → 自动检测断裂的引用和不可达节点
- 集成/Playwright → 需跟着更新

## 当前进度

- [x] 项目初始化
- [x] 类型定义
- [x] DataLoader
- [x] GameState（含 HP）
- [x] EventResolver（含 matchType）
- [x] EffectApplier
- [x] EndingJudge
- [x] GameContext（React 状态管理）
- [x] 最小 UI（全部 6 个组件）
- [x] 占位数据跑通 3 条路径
- [x] 测试体系建立（70 tests）
- [ ] 存档系统
- [ ] 视觉打磨
- [ ] 内容填充（12 节点 / 15 事件 / 完整书单）
- [ ] 音效
- [ ] 超级匹配高光反馈

## 常用命令

```bash
npm run dev        # 启动开发服务器 (localhost:5173)
npm run build      # TypeScript 编译 + 生产构建
npm test           # 运行 vitest 单元/集成测试
npm run test:e2e   # 运行 Playwright 浏览器测试
```
